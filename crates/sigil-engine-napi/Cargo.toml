[package]
name = "sigil-engine-napi"
version = "0.1.4"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { version = "2", features = ["napi4"] }
napi-derive = "2"
sigil-engine = { path = "../sigil-engine" }

[build-dependencies]
napi-build = "2"
